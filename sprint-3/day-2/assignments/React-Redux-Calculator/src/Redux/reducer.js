import * as types from "./actionType";

const initialState = {
      result: '0',
      operation:"",
      prevValue: "",
      nextValue: ""
};

const reducer = (state = initialState, { type, payload }) => {
      switch(type){
            // case "1": return {...state, state.result:  , state.operation:  , state.prevValue:  , state.nextValue:  }
      }
      
};

export { reducer };
